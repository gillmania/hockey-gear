# Expo SDK 57 has a peer-dependency conflict (react-server-dom-webpack).
# EAS Build installs dependencies in the cloud and reads this file, so the
# --legacy-peer-deps flag we use locally is applied there too. Without it,
# the cloud `npm install` fails.
legacy-peer-deps=true
